name: govulncheck

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly Monday 07:23 UTC — off-hour minute to avoid :00/:30 clustering.
    - cron: '23 7 * * 1'

permissions:
  contents: read

jobs:
  govulncheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache: true

      - uses: golang/govulncheck-action@v1
        with:
          go-version-input: '1.23'
          check-latest: true
